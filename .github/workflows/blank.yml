name: NgShop

on:
  pull_request:
    branches:
    - develop


jobs:
  build:

    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [10.x, 12.x]

    steps:
    - name: npm install, build
      run: |
        npm install
        npm run lint
